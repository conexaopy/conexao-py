"use client";
/* eslint-disable react-hooks/set-state-in-effect, @typescript-eslint/no-explicit-any */

import { useCallback, useEffect, useState } from "react";
import { AdminNav } from "./AdminNav";

type Coupon = { id: string; code: string; description: string | null; discount_type: string; discount_value: number; minimum_order: number; maximum_discount: number | null; usage_limit: number | null; used_count: number; starts_at: string | null; expires_at: string | null; active: boolean };
const empty = { id: "", code: "", description: "", discount_type: "PERCENTAGE", discount_value: 0, minimum_order: 0, maximum_discount: "", usage_limit: "", starts_at: "", expires_at: "", active: true };
export function AdminCoupons() { const [items, setItems] = useState<Coupon[]>([]); const [form, setForm] = useState<any>(empty); const [message, setMessage] = useState(""); const load = useCallback(async () => { const r = await fetch("/api/admin/coupons", { cache: "no-store" }); const data = await r.json(); if (r.ok) setItems(data.coupons); else setMessage(data.error); }, []); useEffect(() => { void load(); }, [load]); const save = async (event: React.FormEvent) => { event.preventDefault(); const r = await fetch("/api/admin/coupons", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(form) }); const data = await r.json(); setMessage(r.ok ? "Cupom salvo." : data.error); if (r.ok) { setForm(empty); await load(); } }; return <main className="min-h-screen bg-[#090a0c] px-5 py-8 text-white lg:px-8"><div className="mx-auto max-w-7xl"><h1 className="text-4xl font-black">Cupons</h1><AdminNav /><form onSubmit={save} className="grid gap-4 rounded-2xl border border-white/10 bg-[#101216] p-6 md:grid-cols-4"><input required value={form.code} onChange={(e) => setForm({ ...form, code: e.target.value })} placeholder="Código" className="admin-input" /><select value={form.discount_type} onChange={(e) => setForm({ ...form, discount_type: e.target.value })} className="admin-input"><option value="PERCENTAGE">Percentual</option><option value="FIXED">Valor fixo</option></select><input required type="number" min="0" step="0.01" value={form.discount_value} onChange={(e) => setForm({ ...form, discount_value: Number(e.target.value) })} placeholder="Valor" className="admin-input" /><input type="number" min="0" step="0.01" value={form.minimum_order} onChange={(e) => setForm({ ...form, minimum_order: Number(e.target.value) })} placeholder="Pedido mínimo" className="admin-input" /><input type="number" min="0" step="0.01" value={form.maximum_discount} onChange={(e) => setForm({ ...form, maximum_discount: e.target.value })} placeholder="Desconto máximo" className="admin-input" /><input type="number" min="0" step="1" value={form.usage_limit} onChange={(e) => setForm({ ...form, usage_limit: e.target.value })} placeholder="Limite de uso" className="admin-input" /><input type="date" value={form.starts_at} onChange={(e) => setForm({ ...form, starts_at: e.target.value })} className="admin-input" /><input type="date" value={form.expires_at} onChange={(e) => setForm({ ...form, expires_at: e.target.value })} className="admin-input" /><label className="flex items-center gap-2 text-xs font-bold"><input type="checkbox" checked={form.active} onChange={(e) => setForm({ ...form, active: e.target.checked })} />Ativo</label><button className="rounded-lg bg-red-600 px-5 py-3 text-xs font-black">{form.id ? "ATUALIZAR" : "CRIAR CUPOM"}</button></form>{message && <p className="mt-4 text-sm text-zinc-400">{message}</p>}<div className="mt-8 grid gap-3">{items.map((item) => <div key={item.id} className="flex flex-wrap items-center justify-between gap-4 rounded-xl border border-white/10 bg-[#101216] p-5"><div><p className="font-bold">{item.code}</p><p className="mt-1 text-xs text-zinc-500">{item.discount_type}: {item.discount_value} · Usos: {item.used_count}{item.usage_limit == null ? "" : `/${item.usage_limit}`}</p></div><button onClick={() => setForm({ ...item, starts_at: item.starts_at?.slice(0, 10) ?? "", expires_at: item.expires_at?.slice(0, 10) ?? "", maximum_discount: item.maximum_discount ?? "", usage_limit: item.usage_limit ?? "" })} className="rounded border border-white/15 px-3 py-2 text-xs font-bold">EDITAR</button></div>)}</div></div></main>; }
