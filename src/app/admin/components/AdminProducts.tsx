"use client";

import Link from "next/link";
import { useCallback, useEffect, useState } from "react";
import { AdminNav } from "./AdminNav";

const statuses = ["PRONTA ENTREGA", "SOB ENCOMENDA", "OFERTA", "NOVIDADE", "MAIS VENDIDO"];
type Category = { id: string; name: string; slug: string };
type Product = { id: string; slug: string; sku: string; name: string; category_id: string; presentation: string | null; description: string | null; price: number; promo_price: number | null; stock_quantity: number; status: string; image_url: string | null; active: boolean; featured: boolean; bestseller: boolean; is_new: boolean; updated_at: string; category?: { name: string } | { name: string }[] | null };
type Form = Omit<Product, "id" | "updated_at" | "category">;
const emptyForm: Form = { slug: "", sku: "", name: "", category_id: "", presentation: "", description: "", price: 0, promo_price: null, stock_quantity: 0, status: "PRONTA ENTREGA", image_url: null, active: true, featured: false, bestseller: false, is_new: false };
const money = (value: number | null) => value == null ? "-" : value.toLocaleString("pt-BR", { style: "currency", currency: "BRL" });
const slugify = (value: string) => value.normalize("NFD").replace(/[\u0300-\u036f]/g, "").toLowerCase().trim().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "");

export function AdminProducts() { const [products, setProducts] = useState<Product[]>([]); const [categories, setCategories] = useState<Category[]>([]); const [form, setForm] = useState<Form>(emptyForm); const [editing, setEditing] = useState<string | null>(null); const [search, setSearch] = useState(""); const [filters, setFilters] = useState({ category: "", active: "", status: "", featured: "" }); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [error, setError] = useState(""); const [notice, setNotice] = useState(""); const load = useCallback(async () => { setLoading(true); const params = new URLSearchParams({ ...filters }); if (search) params.set("search", search); const response = await fetch(`/api/admin/products?${params}`, { cache: "no-store" }); const result = await response.json(); if (response.ok) { setProducts(result.products); setCategories(result.categories); setError(""); } else setError(result.error ?? "Não foi possível carregar produtos."); setLoading(false); }, [filters, search]); useEffect(() => { const frame = window.requestAnimationFrame(() => { void load(); }); return () => window.cancelAnimationFrame(frame); }, [load]); const setField = (key: keyof Form, value: string | number | boolean | null) => setForm((current) => ({ ...current, [key]: value })); const startNew = () => { setEditing(null); setForm({ ...emptyForm, category_id: categories[0]?.id ?? "" }); setNotice(""); }; const edit = (product: Product) => { setEditing(product.id); setForm({ slug: product.slug, sku: product.sku, name: product.name, category_id: product.category_id, presentation: product.presentation ?? "", description: product.description ?? "", price: product.price, promo_price: product.promo_price, stock_quantity: product.stock_quantity, status: product.status, image_url: product.image_url, active: product.active, featured: product.featured, bestseller: product.bestseller, is_new: product.is_new }); setNotice(""); window.scrollTo({ top: 0, behavior: "smooth" }); }; const save = async (event: React.FormEvent) => { event.preventDefault(); setSaving(true); setError(""); setNotice(""); const response = await fetch(editing ? `/api/admin/products/${editing}` : "/api/admin/products", { method: editing ? "PATCH" : "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(form) }); const result = await response.json(); if (!response.ok) setError(result.error ?? "Não foi possível salvar."); else { setNotice(editing ? "Produto atualizado no Supabase." : "Produto criado no Supabase."); setEditing(null); setForm({ ...emptyForm, category_id: categories[0]?.id ?? "" }); await load(); } setSaving(false); }; const quickUpdate = async (product: Product, patch: Partial<Form>) => { const response = await fetch(`/api/admin/products/${product.id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ...product, ...patch }) }); if (response.ok) await load(); else { const result = await response.json(); setError(result.error ?? "Não foi possível atualizar."); } }; const categoryName = (category: Product["category"]) => Array.isArray(category) ? category[0]?.name ?? "-" : category?.name ?? "-"; return <main className="min-h-screen bg-[#090a0c] px-5 py-8 text-white lg:px-8"><div className="mx-auto max-w-7xl"><header className="flex flex-wrap items-end justify-between gap-5 border-b border-white/10 pb-8"><div><p className="text-xs font-bold uppercase tracking-[.3em] text-red-500">Operação</p><h1 className="mt-3 text-4xl font-black">Produtos</h1></div><div className="flex gap-3"><Link href="/" className="rounded-lg border border-white/15 px-4 py-3 text-xs font-bold">Ver loja</Link><button onClick={() => { void fetch("/api/admin/logout", { method: "POST" }); window.location.href = "/admin/login"; }} className="rounded-lg border border-white/15 px-4 py-3 text-xs font-bold text-zinc-400">Sair</button></div></header><AdminNav /><form onSubmit={save} className="rounded-2xl border border-white/10 bg-[#101216] p-6"><div className="flex flex-wrap items-center justify-between gap-4"><h2 className="text-xl font-black">{editing ? "Editar produto" : "Novo produto"}</h2><button type="button" onClick={startNew} className="rounded-lg border border-white/15 px-4 py-3 text-xs font-bold">NOVO PRODUTO</button></div><div className="mt-6 grid gap-4 md:grid-cols-3"><label className="text-xs font-bold uppercase text-zinc-400">Nome<input required value={form.name} onChange={(e) => { setField("name", e.target.value); if (!editing && !form.slug) setField("slug", slugify(e.target.value)); }} className="admin-input" /></label><label className="text-xs font-bold uppercase text-zinc-400">Slug<input required value={form.slug} onChange={(e) => setField("slug", slugify(e.target.value))} className="admin-input" /></label><label className="text-xs font-bold uppercase text-zinc-400">SKU<input value={form.sku} onChange={(e) => setField("sku", e.target.value)} className="admin-input" /></label><label className="text-xs font-bold uppercase text-zinc-400">Categoria<select required value={form.category_id} onChange={(e) => setField("category_id", e.target.value)} className="admin-input">{categories.map((category) => <option key={category.id} value={category.id}>{category.name}</option>)}</select></label><label className="text-xs font-bold uppercase text-zinc-400">Apresentação<input value={form.presentation ?? ""} onChange={(e) => setField("presentation", e.target.value)} className="admin-input" /></label><label className="text-xs font-bold uppercase text-zinc-400">Status<select value={form.status} onChange={(e) => setField("status", e.target.value)} className="admin-input">{statuses.map((status) => <option key={status}>{status}</option>)}</select></label><label className="text-xs font-bold uppercase text-zinc-400">Preço<input required type="number" min="0.01" step="0.01" value={form.price} onChange={(e) => setField("price", Number(e.target.value))} className="admin-input" /></label><label className="text-xs font-bold uppercase text-zinc-400">Preço promocional<input type="number" min="0" step="0.01" value={form.promo_price ?? ""} onChange={(e) => setField("promo_price", e.target.value === "" ? null : Number(e.target.value))} className="admin-input" /></label><label className="text-xs font-bold uppercase text-zinc-400">Estoque<input required type="number" min="0" step="1" value={form.stock_quantity} onChange={(e) => setField("stock_quantity", Number(e.target.value))} className="admin-input" /></label><label className="text-xs font-bold uppercase text-zinc-400 md:col-span-2">URL da imagem<input type="url" value={form.image_url ?? ""} onChange={(e) => setField("image_url", e.target.value || null)} placeholder="https://..." className="admin-input" /></label><label className="text-xs font-bold uppercase text-zinc-400 md:col-span-3">Descrição<textarea value={form.description ?? ""} onChange={(e) => setField("description", e.target.value)} className="admin-input min-h-24" /></label></div><div className="mt-5 flex flex-wrap gap-5 text-xs font-bold text-zinc-400">{([["active", "Ativo"], ["featured", "Destaque"], ["bestseller", "Mais vendido"], ["is_new", "Novidade"]] as const).map(([key, label]) => <label key={key} className="flex items-center gap-2"><input type="checkbox" checked={form[key]} onChange={(e) => setField(key, e.target.checked)} />{label}</label>)}</div><div className="mt-6 flex items-center gap-4"><button disabled={saving} className="rounded-lg bg-red-600 px-5 py-3 text-xs font-black disabled:opacity-60">{saving ? "SALVANDO..." : "SALVAR PRODUTO"}</button>{notice && <span className="text-sm text-emerald-400">{notice}</span>}{error && <span className="text-sm text-red-400">{error}</span>}</div></form><section className="mt-8"><div className="flex flex-col gap-3 md:flex-row"><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Buscar nome, SKU ou slug" className="admin-input flex-1" /><select value={filters.category} onChange={(e) => setFilters({ ...filters, category: e.target.value })} className="admin-input"><option value="">Todas categorias</option>{categories.map((category) => <option key={category.id} value={category.id}>{category.name}</option>)}</select><select value={filters.active} onChange={(e) => setFilters({ ...filters, active: e.target.value })} className="admin-input"><option value="">Ativo/inativo</option><option value="true">Ativos</option><option value="false">Inativos</option></select><select value={filters.status} onChange={(e) => setFilters({ ...filters, status: e.target.value })} className="admin-input"><option value="">Todos status</option>{statuses.map((status) => <option key={status}>{status}</option>)}</select><select value={filters.featured} onChange={(e) => setFilters({ ...filters, featured: e.target.value })} className="admin-input"><option value="">Destaque</option><option value="true">Em destaque</option><option value="false">Sem destaque</option></select></div>{loading ? <p className="mt-8 text-sm text-zinc-500">Carregando produtos...</p> : <div className="mt-6 overflow-x-auto rounded-2xl border border-white/10"><table className="w-full min-w-[1150px] text-left text-sm"><thead className="bg-[#101216] text-xs uppercase tracking-wider text-zinc-500"><tr>{["Imagem", "Nome", "Categoria", "Apresentação", "Preço", "Promoção", "Estoque", "Status", "Ativo", "Destaque", "Atualização", "Ações"].map((label) => <th key={label} className="px-4 py-4">{label}</th>)}</tr></thead><tbody className="divide-y divide-white/10">{products.map((product) => <tr key={product.id}><td className="px-4 py-4">{product.image_url ? <img src={product.image_url} alt="" className="h-12 w-12 rounded object-cover" /> : <div className="flex h-12 w-12 items-center justify-center rounded bg-white/5 text-[9px] text-zinc-600">SEM IMG</div>}</td><td className="px-4 py-4"><p className="font-bold">{product.name}</p><p className="mt-1 text-xs text-zinc-600">{product.sku} · {product.slug}</p></td><td className="px-4 py-4 text-zinc-400">{categoryName(product.category)}</td><td className="px-4 py-4 text-zinc-400">{product.presentation || "-"}</td><td className="px-4 py-4 font-bold">{money(product.price)}</td><td className="px-4 py-4 text-zinc-400">{money(product.promo_price)}</td><td className="px-4 py-4">{product.stock_quantity}</td><td className="px-4 py-4 text-xs">{product.status}</td><td className="px-4 py-4"><button onClick={() => void quickUpdate(product, { active: !product.active })} className={product.active ? "text-emerald-400" : "text-zinc-600"}>{product.active ? "ATIVO" : "INATIVO"}</button></td><td className="px-4 py-4"><button onClick={() => void quickUpdate(product, { featured: !product.featured })} className={product.featured ? "text-amber-300" : "text-zinc-600"}>{product.featured ? "SIM" : "NÃO"}</button></td><td className="px-4 py-4 text-xs text-zinc-500">{new Date(product.updated_at).toLocaleDateString("pt-BR")}</td><td className="px-4 py-4"><button onClick={() => edit(product)} className="rounded border border-white/15 px-3 py-2 text-xs font-bold">EDITAR</button></td></tr>)}</tbody></table>{!products.length && <p className="p-10 text-center text-sm text-zinc-500">Nenhum produto encontrado.</p>}</div>}</section></div></main>; }
